<style>
    table#week {
        border-collapse: collapse;
    }
    table#week tr:nth-of-type(even) {
        background-color: white;
    }
</style>
<div id="table" class="block">
    <table id="week">
        <thead>
        <tr>
            <th></th>
            <th>
                <i class="wi wi-rain-mix"></i>
            </th>
            <th>
                <i class="wi wi-thermometer"></i>
            </th>
            <th>
                <i class="wi wi-cloudy-windy"></i>
            </th>
        </tr>
        </thead>
        <tbody>
        {% for day in forecastData.getDays(5) %}
            <tr>
                {% if loop.index == 1 %}
                    <td>Vandaag</td>
                {% elseif loop.index == 2 %}
                    <td>Morgen</td>
                {% else %}
                    <td>{{ day.getDayname() }}</td>
                {% endif %}
                <td>
                    <span class="value">{{ day.getRain() }}</span>
                    <span class="suffix">mm</span>
                </td>
                <td>
                    <span class="value">{{ day.getTemp() }}</span>
                    <span class="suffix">&deg;C</span>
                </td>
                <td>
                    <span class="value">{{ day.getWindSpeed() }}</span>
                    <span class="suffix">km/h</span>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>
